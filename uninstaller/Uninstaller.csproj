<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>uninstall</AssemblyName>
    <RootNamespace>OtpManager.Setup</RootNamespace>
    <Version>1.0.4</Version>
    <Product>OTP Manager</Product>
    <AssemblyTitle>OTP Manager Uninstaller</AssemblyTitle>
    <Company>Shirogane Kotora</Company>
    <Copyright>Copyright (C) 2026 Shirogane Kotora</Copyright>
    <ApplicationIcon>..\src\Resources\OtpManager.ico</ApplicationIcon>

    <PublishAot>true</PublishAot>
    <InvariantGlobalization>true</InvariantGlobalization>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <!-- Shared with the installer so the two cannot drift apart about what was created. -->
    <Compile Include="..\installer\Setup.cs" Link="Setup.cs" />
  </ItemGroup>

</Project>
